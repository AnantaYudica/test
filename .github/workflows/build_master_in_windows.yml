
name: Build master in Windows

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: Create Build Directory
      run: mkdir -p .build

    - name: Print cmake version
      run: cmake --version
